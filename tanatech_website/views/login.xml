<odoo>
    <template id="auth_login" inherit_id="auth_oauth.login" name="OAuth Login buttons">
        <xpath expr="//t[@t-call='auth_oauth.providers']" position="replace"/>
    </template>

    <template id="auth_signup" inherit_id="auth_oauth.signup" name="OAuth Signup buttons">
        <xpath expr="//t[@t-call='auth_oauth.providers']" position="replace"/>
    </template>

    <template id="login_custom_layout" inherit_id="web.login" name="Login Page with Background Image">
        <!-- Tout le contenu principal -->
        <xpath expr="//form" position="replace">
            <div class="row m-0 vh-100">
                <!-- Colonne gauche : formulaire -->
                <div class="col-md-6 bg-white">
                    <form style="" t-attf-class="oe_login_form_tantech oe_login_form #{'' if login else 'd-none'}" role="form" t-attf-action="/web/login" method="post" onsubmit="this.action = '/web/login' + location.hash">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <div class="text-center">
                            <h2 class="title_login">Log In</h2>
                            <div class="o_login_auth"/>
                        </div>

                        <div class="mb-3" t-if="databases and len(databases) &gt; 1">
                            <label for="db" class="col-form-label">Database</label>
                            <div class="input-group">
                                <input type="text" name="db" t-att-value="request.db" id="db" class="form-control" required="required" readonly="readonly"/>
                                <a role="button" href="/web/database/selector" class="btn btn-secondary">Select <i class="fa fa-database" role="img" aria-label="Database" title="Database"></i>
                                </a>
                            </div>
                        </div>

                        <div class="mb-3 field-login">
                            <label for="login" class="form-label d-flex justify-content-between">Email</label>
                            <input type="text" placeholder="Email" name="login" t-att-value="login" id="login" class="form-control" required="required" autocapitalize="off" autocomplete="username"/>
                        </div>

                        <div class="mb-3">
                            <label for="password" class="form-label d-flex justify-content-between">
                        Password 
                                <a t-if="reset_password_enabled" class="btn btn-link btn-sm" tabindex="1" t-attf-href="/web/reset_password?{{ keep_query() }}">Forgot Your Password?</a>
                            </label>
                            <input type="password" placeholder="Password" name="password" id="password" class="form-control" required="required" autocomplete="current-password" t-att-autofocus="'autofocus' if login else None" maxlength="4096"/>
                        </div>


                        <p class="alert alert-danger" t-if="error" role="alert">
                            <t t-esc="error"/>
                        </p>
                        <p class="alert alert-success" t-if="message" role="status">
                            <t t-esc="message"/>
                        </p>


                        <div class="oe_login_buttons text-center gap-1 d-grid mb-1 pt-3">
                            <button type="submit" class="btn btn-primary">Log in</button>
                            <t t-call="auth_oauth.providers"/>
                            <p class="mt-2 text-center">
                                <a class="smaller text-decoration-none">Don't have an account?</a>
                                <a t-if="signup_enabled" class="btn btn-link btn-sm" t-attf-href="/web/signup?{{ keep_query() }}">sign up</a>
                            </p>
                            <t t-if="debug">
                                <button type="submit" name="redirect" value="/web/become" class="btn btn-link btn-sm">Log in as superuser</button>
                            </t>
                            <div class="o_login_auth"/>
                        </div>

                        <input type="hidden" name="type" value="password"/>
                        <input type="hidden" name="redirect" t-att-value="redirect"/>
                    </form>
                </div>

                <!-- Colonne droite : image -->
                <div class="col-md-6 d-none d-md-block p-0 background">
                </div>
            </div>
        </xpath>
    </template>

    <template id="auth_signup_custom_layout" inherit_id="auth_signup.signup" name="Sign up login">
        <xpath expr="//form" position="replace">
            <div class="row m-0 vh-100">
                <!-- Colonne gauche : formulaire -->
                <div class="col-md-6 bg-white">
                    <form class="oe_login_form_tantech oe_signup_form" role="form" method="post" t-if="not message">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <div class="text-center">
                            <h2 class="title_login">Sign Up</h2>
                            <div class="o_login_auth"/>
                        </div>
                        <!-- <t t-call="auth_signup.fields"> -->
                        <t t-set="only_passwords" t-value="bool(token and not invalid_token)"/>
                        <!-- </t> -->
                        <div class="mb-3 field-login">
                            <label for="login">Email</label>
                            <input type="text" name="login" t-att-value="login" id="login" class="form-control" autofocus="autofocus" autocapitalize="off" required="required" t-att-readonly="'readonly' if only_passwords else None"/>
                        </div>

                        <div class="mb-3 field-name">
                            <label for="name">Name</label>
                            <input type="text" name="name" t-att-value="name" id="name" class="form-control" placeholder="e.g. John Doe" required="required" t-att-readonly="'readonly' if only_passwords else None" t-att-autofocus="'autofocus' if login and not only_passwords else None" />
                        </div>

                        <div class="mb-3 field-password pt-2">
                            <label for="password">Password</label>
                            <input type="password" name="password" id="password" class="form-control" required="required" t-att-autofocus="'autofocus' if only_passwords else None"/>
                        </div>

                        <div class="mb-3 field-confirm_password">
                            <label for="confirm_password">Confirm Password</label>
                            <input type="password" name="confirm_password" id="confirm_password" class="form-control" required="required"/>
                        </div>

                        <p class="alert alert-danger" t-if="error" role="alert">
                            <t t-esc="error"/>
                        </p>
                        <input type="hidden" name="redirect" t-att-value="redirect"/>
                        <input type="hidden" name="token" t-att-value="token"/>
                        <div class="text-center oe_login_buttons d-grid pt-3">
                            <button type="submit" class="btn btn-primary"> Sign up</button>
                            <t t-call="auth_oauth.providers"/>
                            <a t-attf-href="/web/login?{{ keep_query() }}" class="btn btn-link btn-sm" role="button">Already have an account?</a>
                            <div class="o_login_auth"/>
                        </div>
                    </form>
                </div>

                <!-- Colonne droite : image -->
                <div class="col-md-6 d-none d-md-block p-0 background">
                </div>
            </div>
        </xpath>
    </template>

</odoo>