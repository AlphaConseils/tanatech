<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        MCB Hosted Checkout redirect template.
        Injected inline by payment_mcb_form.js; the inline script redirects
        the browser to MCB's hosted checkout page (mcb.gateway.mastercard.com).
        No JS or iframes from MCB's domain are loaded on the merchant page,
        so ad-blockers cannot interfere with payment data capture.
    -->
    <template id="payment_form" name="MCB - Hosted Checkout Redirect">
        <div class="text-center p-4">
            <div class="spinner-border text-primary mb-3" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
            <p class="text-muted small">Redirecting to secure MCB payment page...</p>
        </div>
        <script type="text/javascript">
            (function () {
                var checkoutUrl = '<t t-esc="checkout_url"/>';
                window.location.href = checkoutUrl;
            }());
        </script>
    </template>
</odoo>
